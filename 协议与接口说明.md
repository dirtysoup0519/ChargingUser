# 充电数据服务端 —— 协议与接口说明（v2.6）

> 本文档对应工程 `DatabaseSideProject`（Qt Widgets + QtSql/QtNetwork，SQLite）。
> 协议层（`protocol.h` + `massagehandler.h/.cpp`）为客户端与服务器端**共同遵循**，
> 客户端工程直接拷贝这三个文件即可完成接入。
> v2 变更：新增业务层 ServiceManager（充电状态机/钱包/事务）、手机号免密登录、
> 金额统一整数分、电桩双状态分离、设备登记制，详见文末版本记录。
> v2.5 变更：一般注册/账户维护/电站电桩查询与管理协议 + 306/307 错误类型。

---

## 1. 报文格式

```
+----------------+----------------+----------------------+
| msgType (4B)   | msgSize (8B)   | msg (msgSize 字节)    |
+----------------+----------------+----------------------+
      大端整数           大端整数          UTF-8 JSON 文本
```

- `msgType`：消息类型码（三位数字分段：1xx 请求 / 2xx 应答 / 3xx 错误 / 4xx 大数据控制）
- `msgSize`：msg 字段字节数，可为 0（心跳帧）
- `msg`：业务载荷，`QJsonObject` 序列化后的 Compact JSON；字段见第 4 节

**粘包/分片处理**：解析端维护缓冲区，逐帧剥离；载荷超过 `BIGDATA_THRESHOLD`(16KB)
自动按 `BIGDATA_START/MID/END`(400/401/402) 分片，接收端自动重组后按原始类型码
交付上层，上层无感知。单帧及分片重组后的完整业务消息上限均为 8MB，分别由
`MAX_MSG_SIZE` 和 `MAX_ASSEMBLED_MSG_SIZE` 约束。

**金额约定**：数据库与业务层统一整数"分"（`balanceCents`/`amountCents`/`priceCents`），
电量功率用 double；协议载荷**冗余携带元与分双字段**（如 `amount` + `amountCents`），
客户端展示用元、业务校验用分。订单金额使用开始充电时的价格快照（`priceCentsSnapshot`），
充电期间调价不影响进行中订单。

## 2. 业务分层

```
手机端/设备/PC端 请求
        │ (TCP, v2 协议)
        ▼
网络层  SocketThreadManager（监听/会话/分发/设备上下线/心跳超时）
        + MassageHandler（帧编解码/粘包/分片，两端共用）
        ▼
业务层  ServiceManager（订单结算、完整性校验、事务处理；QObject，可扩展子类）
        ▼
数据层  DataBaseManager（SQL 唯一出口，事务接口 begin/commit/rollback）
        ▼
SQLite  Server.db
```

- PC 管理端同机部署也统一走 TCP（本机回环 127.0.0.1），经协议接入，业务校验只做一份；
- ServiceManager 是服务器内部业务层，不直接暴露给任何客户端进程；
- 手机端/电桩经协议进入网络层，由 `SocketThreadManager` 转调 ServiceManager；
- 通用增删改查（110/111/112）为**高权限管理/调试通道**，保留且不做安全校验。

## 3. 消息类型码总表

| 类型码 | 宏 | 方向 | 说明 |
|---|---|---|---|
| 100 | GETDATA | C→S | 通用查询（预留） |
| 101 | LOGIN_REQ | C→S | 账号密码登录（管理员 & 兼容） |
| 102 | LOGOUT_REQ | C→S | 退出登录（预留） |
| 105 | CHGDATA_REQ | 设备→S | 充电数据**实时上报**（v2 语义，见 4.4） |
| 106 | ORDERQRY_REQ | C→S | 订单查询 |
| 107 | HEARTBEAT | C→S | 心跳（空载荷） |
| 108 | START_CHARGING_REQ | 用户→S | 开始充电 |
| 109 | STOP_CHARGING_REQ | 用户→S | 停止充电 |
| 110 | ADDDATA | C→S | 新增记录（高权限通道） |
| 114 | RESTART_REQ | PC管理端→S | 远程重启电桩（管理员） |
| 111 | UPDDATA | C→S | 修改记录（高权限通道） |
| 112 | DELDATA | C→S | 删除记录（高权限通道） |
| 113 | RECHARGE_REQ | 用户→S | 钱包充值 |
| 115 | PAY_REQ | 用户→S | 订单支付 |
| 116 | PHONE_LOGIN_REQ | 手机端→S | 手机号免密登录（不存在自动注册） |
| 117 | REG_REQ | C→S | **一般注册**（v2.5，用户名+密码，见 4.11） |
| 118 | PROFILE_UPD_REQ | 用户→S | **修改账户**（v2.5，用户名/密码/昵称/头像，见 4.12） |
| 119 | STATION_QRY_REQ | 用户/PC→S | **电站/电桩查询**（v2.5，见 4.13） |
| 124 | STATION_MNG_REQ | PC管理端→S | **电站/电桩管理**（v2.5，增删改，见 4.14） |
| 125 | RESERVE_REQ | 用户→S | **预约充电**（v2.6，押金 20 元/保持 2 小时，见 4.15） |
| 120 | DEV_ONLINE | 设备→S | 电桩上线登记（未登记设备被拒绝） |
| 121 | DEV_OFFLINE | 设备→S | 电桩主动下线 |
| 200 | GETDATA_ACK | S→C | 通用查询结果 {table,cond}（user 表脱敏） |
| 201 | LOGIN_ACK | S→C | 登录成功 |
| 208 | START_CHARGING_ACK | S→用户 | 开始充电成功 |
| 209 | STOP_CHARGING_ACK | S→用户 | 停止充电成功（进入待支付） |
| 210 | ADDSUCCESS | S→C | 新增成功 |
| 211 | UPDSUCCESS | S→C | 修改成功 |
| 212 | DELSUCCESS | S→C | 删除成功 |
| 213 | CHGDATA_ACK | S→设备 | 上报确认（当前订单累计电量/金额） |
| 214 | ORDERQRY_ACK | S→C | 订单查询结果 |
| 215 | PAY_ACK | S→用户 | 支付成功 |
| 216 | RECHARGE_ACK | S→用户 | 充值成功 |
| 217 | PHONE_LOGIN_ACK | S→手机端 | 手机号登录成功 |
| 218 | RESTART_ACK | S→管理员 | 远程重启受理确认 |
| 219 | REG_ACK | S→C | **注册成功**（v2.5） |
| 220 | DEV_ONLINE_ACK | S→设备 | 设备上线确认 |
| 221 | DEV_OFFLINE_NOTICE | S→管理员 | 设备下线广播 |
| 222 | DEV_ONLINE_NOTICE | S→管理员 | 设备上线广播（管理员确认登记后） |
| 223 | PAYMENT_NOTICE | S→用户 | 支付提醒（订单转待支付，含金额/截止时间） |
| 224 | DEV_RESTART_NOTICE | S→设备 | 远程重启通知（设备自行重启后重新上线） |
| 225 | CHG_PROGRESS | S→用户 | 充电进度推送（percent/kwh/金额/预计充满时间） |
| 226 | CHG_FAULT_NOTICE | S→用户 | 充电异常通知（故障中止 + 结算结果） |
| 227 | CHARGER_FAULT_NOTICE | S→管理员 | 电桩故障广播 |
| 228 | PROFILE_UPD_ACK | S→用户 | **账户修改成功**（v2.5） |
| 229 | STATION_QRY_ACK | S→C | **电站/电桩查询结果**（v2.5） |
| 230 | HEARTBEAT_ACK | S→C | 心跳应答 |
| 231 | STATION_MNG_ACK | S→管理员 | **电站/电桩管理操作成功**（v2.5） |
| 232 | RESERVE_ACK | S→用户 | **预约成功**（v2.6，含预约编号/过期时间/余额） |
| 233 | RESERVE_EXPIRED_NOTICE | S→用户 | **预约超时注销通知**（v2.6，押金不退） |
| 234 | CHG_ORDER_NOTICE | S→设备 | **订单状态同步**（v2.6，Reserved/Charging/Idle） |
| 300~307 | DATA_NOEXIST 等 | S→C | 错误（JSON 携带业务码 `code`，见 4.8；v2.5 新增 306/307） |
| 400~402 | BIGDATA_* | 双向 | 大数据分片（自动，透明） |

## 4. 业务消息字段定义（msg 载荷）

### 4.0 通用约定（客户端必读）

**会话与身份**：TCP 连接建立后为"未登录"状态，完成 101/116 登录后成为
`user`（手机端用户）或 `admin`（管理员）会话；设备经 120 上线后成为 `device` 会话。
身份由服务端会话绑定（`ctx.role/ctx.identity`），**不信任载荷里自报的角色/用户名**——
改账户（118）、开始充电（108）等消息中的 username 仅为业务目标，越权由服务端裁决。

**权限矩阵**（✓=服务端强制校验通过；✗=服务端直接拒绝并回错误帧）：

| 消息 | 未登录 | user（手机端） | admin（PC端） | device（电桩） |
|---|---|---|---|---|
| 117 注册 / 101 / 116 登录 | ✓ | ✓ | ✓ | ✗ 301 |
| 118 修改账户 | ✗ 301 | 仅本人 | 任意账户 | ✗ 301 |
| 119 电站/电桩查询 | ✗ 301 | ✓ | ✓ | ✗ 301 |
| 124 电站/电桩管理 | ✗ 301 | ✗ 301 | ✓ | ✗ 301 |
| 100 通用查询 / 106 订单查询 | ✗ 301 | ✓ | ✓ | ✗ 301 |
| 108 开始充电 / 113 充值 / 115 支付 | ✗ 301 | ✓ | ✓（代充/代结算） | ✗ 301 |
| 109 停止充电 | ✓* | ✓ | ✓ | ✓ |
| 110/111/112 通用增删改 | ✗ 301 | ✗ 301 | ✓ | ✗ 301 |
| 114 远程重启 | ✗ 301 | ✗ 301 | ✓ | ✗ 301 |
| 105 上报 / 120 / 121 / 107 心跳 | ✓ | ✓ | ✓ | ✓ |

\* 109 开放给 device：虚拟桩/真实桩满电自动停止时以自身会话发送，业务层按桩状态裁决。
未登录会话收到 ✗ 301 的错误帧携带 `code=AUTH_FAIL`，客户端应引导重新登录。

**通用错误处理流程**：任何请求成功回 2xx ACK（载荷 `ok:true`）；失败回 3xx 错误帧
（载荷 `{code, err}`）——客户端**按 `code` 精确处理、按 `err` 展示兜底文案**；
`AUTH_FAIL`/`USER_FROZEN` 需更新本地登录态/状态显示，其余 toast 即可。

**金额双字段**：所有金额同时携带 `amountCents`（整数分，业务/校验以此为准）与
`amount`（元 double，仅展示）；请求同理（如充值 113 可传任一）。

**头像规范（118 avatar 字段，手机端处理流程）**：
1. 手机端选图后**本地缩放至 ≤128×128、JPEG 质量 60~75**（原图一般压到 5~20KB）；
2. base64 编码，推荐带 data URI 前缀：`data:image/jpeg;base64,/9j/4AAQ...`；
3. **整串长度 ≤96KB**（对应原图约 70KB），超限服务端拒绝并回 305 + err 提示；
4. 发送 118 `{ "username": "自己", "avatar": "<base64串>" }`，成功后 228 `changed` 含 `"avatar"`；
5. 回显：116/217 登录响应与 101/201 登录响应的 `avatar` 字段、100 通用查询 user 表行内
   `avatar` 字段；手机端直接 `data URI` 或加 `data:image/jpeg;base64,` 前缀渲染；
6. 清空头像：发送 `"avatar": ""`；
7. 119 电站/电桩查询**不携带**任何头像（避免列表响应膨胀）；
   订单/管理员用户列表（100）含 avatar，管理端仅在自己需要时查询。

**心跳**：登录成功的客户端每 30s 发 107（空载荷），服务端回 230；90s 无任何数据判掉线。

### 4.1 账号密码登录 101 → 201 / 302（管理员 & 兼容）

```json
{ "username": "admin", "password": "123456", "role": "admin" }
// 成功 201（avatar v2.5 新增回显）
{ "username": "admin", "role": "admin", "avatar": "" }
```
- 字段：username/password 必填；role 请求可省（服务端按库内 role 返回）；
- 失败 302（账号或密码错误）；成功后该连接获得 user/admin 会话，即可发起业务消息；
- **时序要求**：登录成功后客户端立即发一次心跳同步状态，之后每 30s 一次。

### 4.2 手机号免密登录 116 → 217 / 302（手机端）

```json
{ "phone": "13800000000" }
// 成功 217
{ "ok": true, "username": "U13800000000", "phone": "13800000000", "nickname": "13800000000",
  "avatar": "", "status": "Normal", "balanceCents": 0, "balance": 0.0, "autoRegistered": true }
```
11 位手机号；不存在自动注册（`username = "U"+phone`，免密，role=user）。手机号为普通用户唯一业务身份。
`avatar` 即用户头像（data URI base64，见 4.0 头像规范），登录即回显，无需单独查询。

### 4.3 充电状态机 108/109/115（ServiceManager 控制）

```
开始充电 108 → [Charging 订单] → 实时上报 105 → 停止充电 109 → [PendingSettlement 订单]
                                                        ├── 15 分钟内支付 115 → [Settled]
                                                        └── 逾期未付（30s 扫描）→ [Cancelled] + 用户冻结
```

```json
// 开始充电 108（chargerCode 必须已登记且 online 且 businessStatus=空闲；用户未冻结）
{ "username": "U13800000000", "chargerCode": "P001" }
// 成功 208
{ "ok": true, "orderNo": "OD...", "stationName": "东门站", "price": 1.5, "priceCents": 150 }

// 停止充电 109
{ "chargerCode": "P001" }
// 成功 209
{ "ok": true, "orderNo": "OD...", "amountCents": 1875, "amount": 18.75, "kwh": 12.5,
  "paymentDeadline": "2026-09-04 16:05:00" }

// 支付 115（Frozen 用户可支付；管理员会话可对任意用户订单代结算）
{ "orderNo": "OD..." }
// 成功 215
{ "ok": true, "orderNo": "OD...", "amountCents": 1875, "amount": 18.75,
  "balanceCents": 8125, "balance": 81.25 }
```

### 4.4 充电数据实时上报 105 → 213（电桩/stm32，v2 语义）

```json
{ "chargerCode": "P001", "kwh": 12.5 }
// 成功 213（更新当前 Charging 订单的累计电量与金额）
{ "ok": true, "orderNo": "OD...", "username": "U13800000000", "kwh": 12.5,
  "price": 1.5, "priceCents": 150, "amount": 18.75, "amountCents": 1875 }
```
设备为权威数据源：**只认 `chargerCode` + `kwh`**（可选 `percent` 0~100，同步更新订单电量百分比），且必须先 DEV_ONLINE 绑定身份；
stationName/username/订单归属以服务器状态为准。电桩非充电中 → 301/STATE_CONFLICT。

### 4.5 钱包充值 113 → 216

```json
{ "username": "U13800000000", "amount": 50.0 }
// 成功 216：{ "ok": true, "balanceCents": 5000, "balance": 50.0 }
```

- **管理员代充**：管理员会话（role=admin）发送 113 且 `username` 填目标用户即代充，
  服务端按会话身份校验，流水备注记为 `管理员(admin)代充`；用户自充备注为 `钱包充值`。
- 使用要求：须登录会话（未登录 301）；amount/amountCents 至少一项且 >0；
  Frozen 用户**允许充值**（只禁新充电）；无余额上限校验。

### 4.6 订单查询 106 → 214

```json
// 条件均可选；支持 status（Charging/PendingSettlement/Settled/Cancelled）
{ "username": "U13800000000", "status": "PendingSettlement", "stationName": "东门站", "chargerCode": "P001", "orderNo": "OD..." }
// 应答 214（条数不限，分片自动处理）
{ "orders": [ { "orderNo": "OD...", "username": "...", "stationName": "东门站", "chargerCode": "P001",
                "status": "PendingSettlement", "startedAt": "...", "endedAt": "...", "kwh": 12.5,
                "priceCentsSnapshot": 150, "price": 1.5, "amountCents": 1875, "amount": 18.75,
                "paymentDeadline": "...", "settledAt": "", "createdAt": "..." } ] }
```
startedAt/endedAt/settledAt/paymentDeadline 均已持久化；时间范围条件确定后直接扩展 cond。
使用要求：须登录会话；普通用户仅应查自己 username（服务端当前未隔离，客户端自觉传自身条件）。

### 4.7 通用增删改 110/111/112（高权限通道）→ 210/211/212

```json
// 新增 110（table: user / station / charger / orderInfo / walletTransaction / reservation）
{ "table": "charger", "record": { "chargerCode": "P001", "stationName": "东门站",
                                  "chargerIndex": 1, "type": "快充", "power": 60 } }
// 修改 111（fields 只写要改的字段；如电站调价：对站内全部电桩生效）
{ "table": "station", "key": { "stationName": "东门站" }, "fields": { "priceCents": 200 } }
// 删除 112
{ "table": "charger", "key": { "chargerCode": "P001" } }
```
使用要求：仅管理员（未登录/user/device 一律 301）；**直通 DB 层、仅做表/主键校验，
无业务规则校验**——电站/电桩维护请优先走 124（4.14，带完整性校验）；110/111/112
保留用于用户管理、TestMonitor 直写等场景。

### 4.8 错误响应（3xx + 业务码）

```json
{ "code": "USER_FROZEN", "err": "账户已冻结，禁止开始新充电" }
```
网络层类型码仍为 3xx（300 不存在 / 301 非法 / 302 登录失败 / 303 冲突 / 304 DB 错误 /
305 参数 / **306 认证失败 / 307 业务禁止**，后两个为 v2.5 新增）；
`code` 业务错误码：`USER_FROZEN` / `INSUFFICIENT_BALANCE` / `CHARGER_UNAVAILABLE` /
`STATE_CONFLICT` / `NOT_FOUND` / `PARAM_ERROR` / `DB_ERROR` /
**`AUTH_FAIL`（旧密码错误/越权改资料）/ `OP_FORBIDDEN`（删非空电站/改充电中桩）/ `DUPLICATE`（用户名/手机号/编号冲突）**
（加粗为 v2.5 新增）。网络层 `errTypeFor()` 统一完成 code → 3xx 类型码映射，
客户端按 code 精确提示、按 err 兜底。

### 4.9 设备上下线 120/121 → 220 / 221

```json
// 上线 120（设备必须已由管理员登记，未登记拒绝：301 + code=NOT_FOUND）
{ "chargerCode": "P001" }
// 应答 220：{ "chargerCode": "P001" }
// 服务端确认登记后，向所有在线管理员广播 222：
{ "chargerCode": "P001" }
// 服务端检测到设备断开/心跳超时时，向所有在线管理员广播 221：
{ "chargerCode": "P001", "reason": "心跳超时" }
```
- 电桩**网络状态 `online` 与业务状态 `businessStatus` 分离**（0 空闲/1 已预约/2 充电中/3 故障/4 重启中）；
- 同一电桩重复上线：服务端踢掉旧连接保留新连接；断开时若仍有其他连接则不置离线；
- **上线准入两档**：自动确认（默认，已登记设备直接回 220 并广播 222）/ 手动确认（服务端主界面
  "设备登记确认"区逐台确认或拒绝，待确认期间 220 携带 `pending:true`，确认后重发完整 220）；
- 服务器启动时全部 online 清零（防崩溃残留假在线），由设备重新 120 恢复。

### 4.10 心跳 107 / 230 与断线重连

客户端每 `HEARTBEAT_INTERVAL_MS`(30s) 发一帧空载荷心跳，服务端回 230；
`HEARTBEAT_TIMEOUT_MS`(90s) 无任何数据判定掉线并清理会话（电桩置离线 + 广播）。
**客户端重连约定**：失败后等 `RECONNECT_INTERVAL_MS`(5s) 重连，重连成功后重新登录
（101/116）或重新上线（120），再继续心跳；业务状态全在数据库，靠查询恢复界面。

### 4.11 一般注册 117 → 219（v2.5，无需登录）

```json
// 117 请求
{ "username": "alice", "password": "123456", "phone": "13800000001", "nickname": "小艾" }
// 219 成功
{ "ok": true, "username": "alice", "phone": "13800000001", "nickname": "小艾" }
```
校验（ServiceManager::registerUser）：username/password 必填；username 3~32 字符且唯一
（重复 → 303 + code=DUPLICATE）；password ≥6 位；phone 选填、11 位且未被占用（303）。
成功后客户端需自行登录（101 或 116）。设备会话（role=device）请求将被 301 拒绝。

### 4.12 修改账户 118 → 228（v2.5，仅本人或管理员）

```json
// 118 请求（全部字段可选，至少一项生效）
{ "username": "alice",                       // 必填：目标账户
  "oldPassword": "123456", "newPassword": "abc12345",   // 改密码（免密账户首次设置免校验旧密码）
  "newUsername": "alice2",                   // 改用户名（唯一性校验 + 订单/流水/预约引用级联更新）
  "nickname": "艾酱",                        // 昵称直更 user 表
  "avatar": "data:image/jpeg;base64,/9j/4AAQSkZJRg..." } // 头像：≤96KB base64，规范见 4.0
// 228 成功
{ "ok": true, "username": "alice2", "changed": ["password","username"] }
```
完整性校验（ServiceManager::updateProfile）：请求方须本人（会话 identity）或 admin，
越权 → 306 + code=AUTH_FAIL；旧密码不符 → 306 + AUTH_FAIL；新用户名被占用 → 303 + DUPLICATE；
头像超 96KB → 305 + err 提示压缩要求。
改名走事务（renameUserCascade）：user 主键与 orderInfo/walletTransaction/reservation 同步。
**头像下发链路**：更新成功后，客户端可从本次 228 后的重登（101/116）avatar 字段、
或 100 通用查询 user 行取回；手机端展示直接将 data URI 交给图片控件即可。

### 4.13 电站/电桩查询 119 → 229（v2.5，用户端与 PC 端共用）

```json
// 119 请求（stationName / chargerCode 都缺省 = 全量站列表）
{ }                                        // 全量
{ "stationName": "东区站" }                 // 单站（内嵌该站全部电桩）
{ "chargerCode": "P001" }                  // 按桩反查所属站
// 229 成功
{ "ok": true, "stations": [ {
    "stationName": "东区站", "address": "...", "longitude": 120.1, "latitude": 30.2,
    "priceCents": 150, "price": 1.5, "chargerCount": 2,
    "chargers": [ { "chargerCode": "P001", "stationName": "东区站", "chargerIndex": 1,
                    "type": "快充", "power": 60.0, "businessStatus": 2,
                    "businessStatusText": "充电中", "online": 1, "onlineText": "在线" } ] } ] }
```
桩不存在 → 300 + NOT_FOUND；站不存在 → 300 + NOT_FOUND。

### 4.14 电站/电桩管理 124 → 231（v2.5，仅管理员；业务校验集中 ServiceManager）

```json
// 124 请求：op = add | update | remove；target = station | charger
{ "op": "add", "target": "station",
  "data": { "stationName": "西区站", "address": "...", "longitude": 120.0,
            "latitude": 30.0, "priceCents": 150 } }          // priceCents 或 price（元）二选一
{ "op": "update", "target": "station",
  "key": { "stationName": "西区站" }, "data": { "price": 1.8 } }  // 调价仅影响新订单（价格快照）
{ "op": "remove", "target": "station", "key": { "stationName": "西区站" } }
{ "op": "add", "target": "charger",
  "data": { "chargerCode": "P101", "stationName": "西区站",
            "type": "快充", "power": 60 } }                  // chargerIndex 缺省站内自动编号
{ "op": "update", "target": "charger",
  "key": { "chargerCode": "P101" }, "data": { "power": 120, "businessStatus": 0 } }
{ "op": "remove", "target": "charger", "key": { "chargerCode": "P101" } }
// 231 成功
{ "ok": true, "op": "add", "target": "charger", "chargerCode": "P101" }
```
完整性校验（ServiceManager::manageStationCharger，失败回对应 3xx + code）：
- station add：站名唯一（303）；电价 >0（305）
- station update：站存在（300）；**不支持改站名**（305）；电价 >0（305）
- station remove：站内仍有电桩 → 307 + OP_FORBIDDEN；有进行中/待支付订单 → 307
- charger add：归属站必须存在（300）；编号唯一（303）；type ∈ {快充,慢充}、power>0（305）
- charger update：**充电中（businessStatus=2）禁止改归属/功率/类型/业务状态**（307）；
  改归属新站必须存在（300）；businessStatus 取值 0~4（305）
- charger remove：充电中禁止删除（307）

### 4.15 预约充电 125 → 232 / 233 与订单状态同步 234（v2.6）

**业务规则（服务端自动完整性校验）**：
1. 预约表 `reservation`：{预约编号 id, 手机号 phone, 电桩编号 chargerCode, 预约时间 reserveAt}
   （另含 username/stationName/depositCents/expireAt/status 冗余列，见 §5）
2. **押金 20 元**：预约成功即从钱包扣除（DEPOSIT 流水）；**正式开始充电后退还**（REFUND 流水，
   预约记录转 Completed）
3. **保持时长最大 2 小时**：超时由服务端自动注销（30s 扫描周期），预约转 Expired、
   电桩回 Idle，**押金不予退还**；推 233 通知用户、234 通知电桩
4. 同一用户同时仅一笔进行中预约；同一电桩同时仅被一人预约（桩 businessStatus=1 已预约）
5. 校验顺序与错误码（125 失败回 307 + code）：
   - 用户不存在 → 300 + NOT_FOUND；账号冻结 → 307 + USER_FROZEN
   - 电桩不存在 → 300 + NOT_FOUND；不在线/充电中/已被预约 → 307 + CHARGER_UNAVAILABLE
   - 已有进行中预约 → 307 + RESERVE_CONFLICT；余额不足押金 → 307 + RESERVE_CONFLICT

```json
// 125 请求（手机用户，会话身份即预约人）
{ "chargerCode": "P101" }
// 232 成功
{ "ok": true, "reserveId": "3", "chargerCode": "P101", "stationName": "西区站",
  "phone": "13800000001", "reserveAt": "2026-09-08 09:50:00",
  "expireAt": "2026-09-08 11:50:00",
  "depositCents": 2000, "deposit": 20.0,
  "balanceCents": 8000, "balance": 80.0 }
// 233 预约超时注销通知（服务端主动推）
{ "reserveId": "3", "chargerCode": "P101", "reserveAt": "...", "expireAt": "..." }
```

**预约后开始充电**：预约本人对已预约桩发 108（START_CHARGING_REQ）走预约通道——
预约转 Completed + 押金退还 + 建 Charging 订单，同一事务完成；其他用户 108 被拒
（307 + CHARGER_UNAVAILABLE "电桩已被其他用户预约"）。

**订单状态同步 234（服务端→电桩设备会话，按 chargerCode 定向推送）**：
电桩据此刷新本地展示；虚拟桩引擎已有轮询可忽略，TestMonitor 收到后打印日志。

```json
{ "chargerCode": "P101", "status": "Reserved", "username": "张三" }          // 预约成功
{ "chargerCode": "P101", "status": "Charging",
  "orderNo": "OD...", "username": "张三" }                                   // 开始充电（108 成功）
{ "chargerCode": "P101", "status": "Idle",
  "orderNo": "OD...", "username": "张三" }                                   // 停止充电（109 成功，订单转待支付）
{ "chargerCode": "P101", "status": "Idle" }                                  // 预约超时注销
```

## 5. 数据库设计 v2（SQLite，Server.db；金额统一分）

| 表 | 字段 | 说明 |
|---|---|---|
| user | username(PK) password role phone nickname avatar balanceCents status createdAt | 共表 + role 区分（admin/user）；phone 为普通用户唯一业务身份；status: Normal/Frozen |
| station | stationName(PK) address longitude latitude priceCents createdAt | priceCents：电站单价（分/度），对站内全部电桩生效 |
| charger | chargerCode(PK) stationName chargerIndex type power businessStatus online createdAt | type: 快充/慢充；businessStatus: 0空闲/1已预约/2充电中/3故障/4重启中；online: 0/1 |
| orderInfo | orderNo(PK) username stationName chargerCode status startedAt endedAt kwh priceCentsSnapshot amountCents paymentDeadline settledAt createdAt | status 状态机：Charging/PendingSettlement/Settled/Cancelled |
| walletTransaction | id(PK自增) username type amountCents balanceAfterCents orderNo remark createdAt | 资金流水：RECHARGE/PAY/REFUND/DEPOSIT |
| reservation | id(PK自增) username phone stationName chargerCode depositCents reserveAt expireAt status createdAt | **预约充电业务（v2.6）**：status 状态机 Active/Completed/Expired/Cancelled；老库启动自动补 phone 列 |

- 初始管理员自动写入：`admin / 123456`（role=admin）
- **schema 自动升级**：`InitTable()` 建表后按 `PRAGMA table_info` 检查缺列并自动
  `ALTER TABLE ADD COLUMN`（带默认值），老库启动自动补列，无需删库重建
- 新增字段方法：CREATE TABLE 加列 + `ensureColumn(...)` 补一条 + 对应表白名单加名

## 6. 服务端对外接口一览

### 6.1 ServiceManager（业务层，单例 `ServiceManager::instance()`）

- `loginByPhone(phone)` 手机号免密登录 + 自动注册
- `registerUser(username, password, phone, nickname)` **v2.5** 一般注册（唯一性/格式校验）
- `updateProfile(requestUser, fields)` **v2.5** 修改账户（用户名/密码/昵称/头像；越权/旧密码校验；改名事务级联）
- `queryStationsFull(stationName, chargerCode)` **v2.5** 电站/电桩查询（站内嵌桩数组/按桩反查）
- `manageStationCharger(op, target, data, key)` **v2.5** 电站/电桩管理（唯一性/存在性/业务规则校验集中于此）
- `createReservation(username, chargerCode)` **v2.6** 预约充电（押金/重复/占用/余额五重校验，事务扣押金 + 桩转 Reserved）
- `onReserveTimeoutScan()` **v2.6** 预约超时扫描：30s 周期，expireAt 已过的 Active 预约注销（押金没收）+ 桩回 Idle，信号 `reserveExpired` 推 233/234
- 信号 `orderSyncToCharger(chargerCode, status, orderNo, username)` **v2.6** 订单状态同步（网络层推 234 给电桩会话）
- `startCharging(username, chargerCode)` / `stopCharging(chargerCode)` 充电开始/停止（事务 + 状态机）
- `reportChargingData(chargerCode, kwh)` 设备实时上报 → 更新当前 Charging 订单
- `payOrder(orderNo, requestUser)`（管理员可代结算）/ `recharge(username, yuan, operatorUser)`（管理员可代充） / `walletInfo(username)` 支付/充值/查询（事务 + 流水）
- `setUserFrozen(username, frozen)` 管理员冻结/解冻
- `onServerStart()` 启动恢复（清假在线 + 补扫逾期）；信号 `businessEvent(msg)` 业务日志
- 逾期扫描：30s 周期，paymentDeadline 已过的 PendingSettlement 订单关单并冻结用户

### 6.2 DataBaseManager（单例，SQL 全部收敛于此）

- `init()` 建库建表 + schema 升级；`lastError()` 最近错误
- 事务：`beginTransaction() / commit() / rollback()`
- 用户：`addUser / removeUser / updateUser / queryUsers(cond) / checkLogin / queryUserByPhone`
- 订单：`addOrder / removeOrder / updateOrder / queryOrders(cond 支持 status)`
- 电站：`addStation / removeStation / updateStation / queryStations(cond)`
- 电桩：`addCharger / removeCharger / updateCharger / queryChargers(cond) / setChargerOnline / setAllChargersOffline`
- 充电兼容：`reportCharging(...)` 旧版"上报即结算"，保留但业务流程已迁至 ServiceManager
- 流水：`addWalletTransaction(...)`
- 检查点：`createCheckpoint() / restoreCheckpoint() / checkpointTime()`
- 管理：`executeRaw(sql)`（SQL 控制台专用）
- 返回值 `DbResult`：`DB_OK / DB_EXIST(303) / DB_NOEXIST(300) / DB_FAIL(304)`

### 6.3 SocketThreadManager（网络服务）

- `startServer(ip, port, &err)` / `stopServer()` / `isRunning()` / `clientCount()`
- 信号：`clientOnline / clientOffline / chargerOnline / chargerOffline / logMessage`
- 内部：连接管理、粘包解析、按类型码分发（业务转 ServiceManager）、
  设备登记校验、重复上线踢旧连接、心跳超时检测

### 6.4 MassageHandler（两端共用）

- 打包：`pack(type, json)` / `pack(type, jsonArr)` / `packError(type, reason, bizCode)`
- 解析：`feed(bytes)` → 信号 `frameReady(msgType, payload)`；`reset()` 重置缓冲
- 业务构造：`makeLogin / makeRecord / makeUpdate / makeDelete / makeOrderQuery /
  makeChargingReport / makeDevOnline / makeDevOffline / makeHeartbeat`

## 7. 客户端接入指引

1. 拷贝 `protocol.h`、`massagehandler.h`、`massagehandler.cpp` 到客户端工程（仅需 QtCore）。
2. 连接服务器（默认 127.0.0.1:12345），`socket->connectToHost(...)`。
3. `readyRead` 中 `handler->feed(socket->readAll())`；连接 `frameReady` 处理业务帧。
4. 发送请求：`socket->write(MassageHandler::pack(LOGIN_REQ, MassageHandler::makeLogin(...)))`。
5. 心跳：QTimer 每 30s `socket->write(MassageHandler::makeHeartbeat())`；
   断线后 5s 重连并重新登录/上线。
6. PC 管理端（含同机部署）：与手机端/设备完全一致，统一走 TCP（本机回环 127.0.0.1:12345）经协议接入，登录后按 role 区分权限；不直接链接 ServiceManager。

## 8. 消息流程示例（v2 充电状态机）

```
手机端用户                      服务器(ServiceManager)           电桩(P001)
    |--- 116 PHONE_LOGIN_REQ --->|                                |
    |<-- 217 PHONE_LOGIN_ACK ----|                                |
    |      (每30s心跳107/230)     |<-- 120 DEV_ONLINE ------------|
    |                            |--- 220 DEV_ONLINE_ACK ------->|
    |--- 108 START_CHARGING ---->|  (校验+建Charging订单+电桩置充电中)
    |<-- 208 START_CHARGING_ACK -|                                |
    |                            |<-- 105 CHGDATA_REQ(实时kwh)----|
    |                            |--- 213 CHGDATA_ACK(累计) ----->|   (可多次)
    |--- 106 ORDERQRY_REQ ------->|                               |
    |<-- 214 ORDERQRY_ACK --------|                                |
    |--- 109 STOP_CHARGING ------>|  (订单转PendingSettlement,15min期限)
    |<-- 209 STOP_CHARGING_ACK ---|                                |
    |--- 115 PAY_REQ ------------>|  (扣款+Settled+流水, 事务)      |
    |<-- 215 PAY_ACK -------------|                                |
    |                     (15分钟未支付→订单Cancelled+用户Frozen)    |
```

## 9. 服务端管理功能

**命名约定**：电桩统一命名 `Charger`（表 `charger`、字段 `chargerCode`/`chargerIndex`、接口 `addCharger/queryChargers/...`）。

**settings 设置**：
- 监听 IP / 端口：保存后停止服务重启生效；IP 留空绑定全部网卡
- 创建检查点：备份当前数据库（Server.db → Server.db.bak，先合并 WAL）
- 恢复到检查点：强制二次确认；服务运行中额外警告（建议先停止服务）

**日志持久化（v2.5）**：主窗口全部日志（网络层 logMessage、业务层 businessEvent、
虚拟桩 virtualEvent）统一收口 `Widget::appendLog`，同步写入可执行文件同目录的
`Server.log`（`yyyy-MM-dd HH:mm:ss.zzz  消息` 逐行 UTF-8）；超过 5MB 自动滚动为
`Server.log.1`（保留一代）。文件打开失败静默降级，界面日志不受影响。

**虚拟桩地址跟随（v2.5）**：VirtualChargerManager 的接入地址与服务器监听配置联动——
监听 IP 留空/0.0.0.0 时虚拟桩自动走 `127.0.0.1` 回环；settings 修改地址保存后，
运行中的虚拟桩全部卸载并在 ≤2s 内按新地址自动重挂，无需重启服务。

**超级管理员控制台**（adminBash 区域 + 底部命令行）：

| 命令 | 作用 |
|---|---|
| help / tables / clear | 帮助 / 列表 / 清屏 |
| checkpoint / restore | 创建检查点 / 恢复（二次确认） |
| 其他输入 | SQL 直执（SELECT 显示结果，其他显示影响行数） |

**并发设计**：DataBaseManager 递归锁（QRecursiveMutex）串行化全部接口 + SQLite
`WAL` + `busy_timeout=5000`；主线程业务模型下事务不被并发插入，事务接口供
ServiceManager 组织多步原子操作。

## 10. 版本记录

- **v2.6（2026-09-08）**：预约充电业务启用 + 订单状态同步——①预约 125→232（手机用户，押金 20 元即扣 DEPOSIT 流水，保持 2 小时，五重校验：冻结/桩在线空闲/一用户一预约/一桩一预约/余额足押金，错误码 RESERVE_CONFLICT 等）；②超时自动注销：30s 扫描 expireAt（持久化，重启不丢），预约转 Expired + 桩回 Idle + **押金不予退还**，推 233 通知用户；③押金退还：预约本人 108 开始充电走预约通道（桩 Reserved 状态放行本人），预约转 Completed + 押金 REFUND 退还，同事务；④订单状态同步 234 CHG_ORDER_NOTICE（服务端→电桩设备会话，按 chargerCode 定向）：预约 Reserved / 开始充电 Charging / 停止充电或预约注销 Idle，TestMonitor 收到打印日志，虚拟桩轮询忽略；⑤DB：reservation 表启用 + phone 列（ensureColumn 老库自动补列），新增 addReservation/updateReservation/queryReservations；⑥网络层新增 notifyCharger（按设备号定向推送）与 errTypeFor 新映射（RESERVE_CONFLICT/CHARGER_UNAVAILABLE/USER_FROZEN → 307）。
- **v2.5（2026-09-07）**：账户与站点协议扩展——①一般注册 117→219（用户名+密码，唯一性/格式校验，无需登录）；②修改账户 118→228（改用户名/密码/昵称/头像同步 user 表，仅本人或管理员，旧密码校验 AUTH_FAIL，改名事务级联 orderInfo/walletTransaction/reservation）；③电站/电桩查询 119→229（用户端与 PC 端共用，站列表内嵌桩数组/按桩反查，冗余 businessStatusText/onlineText）；④电站/电桩管理 124→231（仅管理员，op×target 组合增删改，校验集中 ServiceManager：站非空禁删/有未结订单禁删/充电中桩禁改禁删/编号站名唯一/电价>0，chargerIndex 站内自动编号）；⑤新增错误类型 306 AUTH_ERROR（AUTH_FAIL）与 307 OP_FORBIDDEN（OP_FORBIDDEN）+ 业务码 DUPLICATE，网络层 errTypeFor() 统一 code→3xx 映射；DB 层新增 renameUserCascade/nextChargerIndex。⑥**头像方案落地**：118 avatar 字段 base64 data URI（≤96KB，压缩规范见 4.0），101/201 与 116/217 登录响应新增 avatar 回显，100 user 表行含 avatar（仍脱敏 password）；⑦**服务端强制登录校验**：100/106/108/113/115 未登录会话一律 301+AUTH_FAIL（109 保持开放：设备满电自动停止合法），§4.0 新增通用约定与权限矩阵。
- **v2.4（2026-09-05）**：电桩故障业务闭环——服务端 2s 扫描统一感知置故障（TestMonitor 直写库/vdev/111 全覆盖）：自动停止订单并立即结算（余额不足保持待支付），226 通知用户充电异常与结算结果，227 广播管理员；stopCharging 支持 silent（故障场景不推 223）。
- **v2.3（2026-09-05）**：虚拟充电桩引擎改为 TCP 接入（与真实桩/TestMonitor 同构：120/107/105/109/224 全协议流量，可直接用于大数据/机器学习采集）；vdev 状态控制走管理员会话 111；被外部接管的虚拟桩自动让位。
- **v2.2（2026-09-05）**：充电百分比协议（105 携带 percent、订单表 percent/capacityKwh 列、213/214/225 下发）；虚拟充电桩引擎（Settings 启用挂载，随机初始电量 15~45% 与电池容量，2s tick 推进，100% 自动停止提交订单并推 223）；远程重启 114/218（虚拟桩服务端模拟，TCP 桩转 224）；通用查询 200；管理员代结算/代充；订单满期/故障自动停止。
- **v2.1（2026-09-05）**：设备上线准入（自动/管理员手动确认）+ 222 上线广播；管理员代结算（115）与代充值（113）接口；充值流水 orderNo 空值归一化（NOT NULL 约束修复）。
- **v2（2026-09-04）**：新增 ServiceManager 业务层；充电状态机（Charging→PendingSettlement→Settled，15 分钟支付期限持久化）；手机号免密登录（116）；金额统一整数分（协议双字段冗余）；电桩业务状态与在线状态分离 + 设备登记制 + 多连接保护；五表结构 + schema 自动升级 + 事务接口；错误响应携带业务错误码。
- **v1（2026-09-04）**：协议框架（帧格式/粘包/分片/心跳）、四表、通用 CRUD、充电"上报即结算"（已被 v2 状态机取代，接口保留兼容）、设备上下线、检查点、SQL 控制台。
